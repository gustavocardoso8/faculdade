Algoritmo "FuncaoSomaParam"

funcao soma (x, y: inteiro): inteiro

inicio

      retorne x + y

fimFuncao

Var

   n, m, res: inteiro

Inicio

      escreval("Vamos fazer uma soma?")
      escreva("Por favor, digite um número: ")
      leia(n)
      escreva("Agora, mais um: ")
      leia(m)
      res <- soma(n,m)
      escreva("O resultado da soma é:", res)


Fimalgoritmo